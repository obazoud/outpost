{
  "type": "webhook",
  "config_fields": [
    {
      "key": "url",
      "type": "text",
      "label": "Webhook URL",
      "description": "The URL to send webhook requests to",
      "required": true,
      "pattern": "^https?:\\/\\/(?:localhost|127\\.0\\.0\\.1|\\[::1\\]|(?:[\\w\\-]+\\.)+[a-z]{2,})(?::\\d{1,5})?(?:\\/[\\w\\-\\/\\.~:?#\\[\\]@!$&'\\(\\)*+,;=]*)?$"
    }
  ],
  "credential_fields": [
    {
      "key": "secrets",
      "type": "text",
      "label": "Webhook Secrets",
      "description": "JSON array of webhook secrets. Each secret should have: key (string), created_at (ISO 8601 timestamp), and optionally invalid_at (ISO 8601 timestamp) to manually invalidate the secret before the default 24-hour window",
      "required": false,
      "sensitive": false
    }
  ]
}
