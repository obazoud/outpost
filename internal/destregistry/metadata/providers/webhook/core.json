{
  "type": "webhook",
  "config_fields": [
    {
      "key": "url",
      "type": "string",
      "label": "Webhook URL",
      "description": "The URL to send webhook requests to",
      "required": true
    }
  ],
  "credential_fields": [
    {
      "key": "secrets",
      "type": "string",
      "label": "Webhook Secrets",
      "description": "JSON array of webhook secrets with key and created_at fields",
      "required": false,
      "sensitive": false
    }
  ]
}
