<div class="wrapper">
  <div class="card">
    {
      [
        "X-Acme-Signature: U8JyYKUIIcMCwrd7adE0LhZbvnCt/zzTdHfkHkJ6Xns=",
        "X-Acme-Timestamp: 2014-12-18T00:00:00Z",
        "X-Acme-Idempotency: 9dfcc6e7-6787-47fc-9a0d-127bc35a2f72",
        "X-Acme-Topic: user.created",
        "X-Content-Type: application/json",
      ].reverse().map((line) => {
        return <span class="line">{line}</span>;
      })
    }
  </div>
</div>

<style>
  .wrapper {
    flex: 1;
    position: relative;
    padding-left: var(--spacing-8);
    display: flex;
    flex-direction: column;
    gap: var(--spacing-4);
    overflow: hidden;
    padding-top: 1px;

    &:before {
      content: "";
      position: absolute;
      top: 1px;
      left: 0;
      width: 100%;
      height: calc(100% + 1px);
      background: linear-gradient(
        180deg,
        #141412 0%,
        rgba(20, 20, 18, 0.5) 100%
      );
    }
  }

  .card {
    margin-top: -1px;
    display: flex;
    flex-direction: column;
    justify-content: flex-start;
    align-items: flex-start;
    gap: var(--spacing-1);
    padding: var(--spacing-6);
    padding-top: var(--spacing-2);
    box-shadow: 0px -1px 0px 0px rgba(255, 255, 255, 0.04), 0px 2px 4px 0px rgba(0, 0, 0, 0.32), 0px 4px 8px 0px rgba(0, 0, 0, 0.32), 0px 0px 0px 1px rgba(255, 255, 255, 0.16);
    border-radius: 0;
    border-bottom-left-radius: 6px;
    z-index: -1;
  }

  .line {
    text-wrap: nowrap;
  }
</style>
