<div class="wrapper">
  {
    [
      "user.created",
      "user.updated",
      "user.deleted",
      "order.created",
      "order.updated",
      "order.deleted",
      "order.returned",
      "invoice.created",
      "invoice.updated",
      "invoice.voided",
      "invoice.refunded",
      "invoice.paid",
    ].map((topic, index) => (
      <div class="checklist-row">
        <span class:list={["checkbox", index === 0 ? "checked" : ""]} />
        <span class="regular-s topic">{topic}</span>
      </div>
    ))
  }
</div>

<style>
  .wrapper {
    flex: 1;
    position: relative;
    padding: var(--spacing-8);
    padding-top: 0;
    display: flex;
    flex-direction: column;
    gap: var(--spacing-4);

    &:before {
      content: "";
      position: absolute;
      bottom: 1px;
      left: 0;
      width: 100%;
      height: calc(100% - 32px);
      background: linear-gradient(
        180deg,
        rgba(20, 20, 18, 0.75) 0%,
        #141412 100%
      );
    }
  }

  .checklist-row {
    display: flex;
    align-items: center;
    gap: var(--spacing-4);
  }

  .checkbox {
    border-radius: 4px;
    border: 1px solid var(--outline-neutral);
    width: 20px;
    height: 20px;
  }

  .topic {
    color: var(--foreground-neutral-2);
  }

  .checked {
    border: 1px solid var(--outline-primary-hover);
    background: var(--outline-primary-hover) url("/images/check.svg");
  }
</style>
