/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { destinationsDisable } from "../../funcs/destinationsDisable.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DisableTenantDestinationRequest$inboundSchema,
};

export const tool$destinationsDisable: ToolDefinition<typeof args> = {
  name: "destinations-disable",
  description: `Disable Destination

Disables a previously enabled destination.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await destinationsDisable(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
